A simple repo for the tutorial

Errors on
- using WebStorm IDE
- using command line start "npm start"

In both cases, if you see "Loading.." in your browser and 404 errors in console,
then change the <base> tag in your index.html.

